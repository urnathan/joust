# Test Aloy runs as expected
# we feed this file into a sub aloy which invokes the the shell on this file
# stdout and stderr are then checked via ezio

# RUN: aloy -t $SHELL -g cat -o - $srcdir$src -- $srcdir$src |
# RUN: ezio -p OUT $src |
# RUN: ezio -p ERR $src

# we'll invoke the shell on each word here
echo "$@" >&2
exit 0

and we're done

# OUT: Test run:
# OUT: # Summary of
# OUT: PASS 0

# ERR: Test run:
# ERR-LABEL: Test:0 echo
# ERR-NEXT: ALOY:{:.*}zsh {:.*}/aloy-1 echo
# ERR-NEXT: echo
# ERR-LABEL: Test:1 "$@"
# ERR-LABEL: Test:2 >&2
# ERR-LABEL: Test:3 exit
# ERR-LABEL: Test:4 0
# ERR-LABEL: Test:5 and
# ERR-LABEL: Test:6 we're
# ERR-LABEL: Test:7 done
# ERR-LABEL: Test generator
# ERR-LABEL: Summary of 8 test programs
# ERR: PASS 0
